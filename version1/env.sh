#!/bin/bash
# Alter this path for the location of the socket of your node and run ". env.sh" before running the any other scripts (that depend on the node) 

# Set the path for the node socket here:
export CARDANO_NODE_SOCKET_PATH="/home/rohan/code/plutus-apps/plutus-pab/test-node/testnet/node.sock"
export MAGIC="--testnet-magic 1097911063"
